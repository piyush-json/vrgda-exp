{
  "name": "api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "latest",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "wrangler": "^4.1.0"
  }
}